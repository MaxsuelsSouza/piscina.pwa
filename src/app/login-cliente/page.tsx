{/*
  ============================================
  CÓDIGO ANTIGO - SISTEMA DE AGENDAMENTO
  Comentado em 2026-01-10 - Migração para Lista de Presentes
  ============================================

  Este arquivo continha a página de login do cliente público (autenticação via telefone + data nascimento).
  Código original preservado abaixo para referência:
*/}
{/*
'use client';

import { useState, useEffect, Suspense } from 'react';
import { useRouter, useSearchParams } from 'next/navigation';
import { useClientAuth } from '@/contexts/ClientAuthContext';
import { Toast } from '@/components/Toast';

// ... [Código completo do arquivo original removido por brevidade - 275 linhas] ...
// Ver histórico do git para código completo
*/}

export default function LoginClientePage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-500 to-blue-600">
      <div className="text-white text-center">
        <p className="text-lg">Página não disponível - Em migração para Lista de Presentes</p>
      </div>
    </div>
  );
}
